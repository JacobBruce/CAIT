# Core (required)
fastmcp>=3.0
chromadb>=0.5

# Research & HTTP helpers
wikipedia-api>=0.8
arxiv>=2.0

# Document conversion — markitdown[all] adds PDF, Office, images, etc.
# Use markitdown[pdf] if you only need PDF support beyond plain text.
markitdown[all]>=0.1

# Optional: higher-quality document conversion (use instead of markitdown)
# docling>=2.0

# Optional: scientific computing for repl_exec
# sympy
# scipy
# matplotlib
# plotly
# vispy
